- HERDR MODE: a host can run herdr 0.7.5+ instead of tmux (Host Settings ▸ Backend), adapted as session → deck tile, workspace → window, pane → pane.
  - Deck: one tile per herdr session, workspaces on its spine, live miniatures, agent names, RUNNING / NEEDS YOU, + NEW SESSION and CLOSE, and a HERDR badge on the host rail beside MOSH.
  - Bind: Add Host ▸ BIND carries a TMUX | HERDR choice, so a herdr machine arrives as a herdr host instead of needing a fix in Host Settings.
  - FAQ: the "no sessions on the deck" answer switches its install commands between TMUX and HERDR in place.
  - + TAB: leads with New Session on herdr tabs too — a new session as its own Multiplex tab, agents included, starting in the pane's directory, same as tmux; a second entry, New Tab in Workspace, instead adds a tab to the workspace you're in (same directory rule, herdr moves you to it) with no new Multiplex tab. The deck's New Session Working Directory choice lands on herdr now too.
  - Host Settings: the New session defaults working-directories editor now shows on herdr hosts as well — paths added there feed New Session, the Host widget, and the Open Agent Shortcut.
  - HRDR shortcuts: herdr tabs get their own shortcut panel (splits, tabs, workspaces, confirmed closes, Switch Workspace); tall panels scroll on iPhone.
  - LIVE lamp: a herdr tile reads ● LIVE while one of your own terminal windows is attached to it.
  - FILE attach: FILE and drag-and-drop work on herdr tabs — upload into the focused pane's directory, path typed, never submitted.
  - Agent strip: switch tabs or workspaces inside herdr; the helper chips should follow the focused agent in about one second instead of waiting for the five-second wall refresh.
  - RESIZE BY TOUCH: on a herdr tab, long-press a pane divider line and drag — the split follows your finger (herdr resizes it live). A long press anywhere else keeps its usual meaning (links, selection menu).
  - AGENT HISTORY: on a herdr tab running Claude Code, the strip's HIST opens the same prompt list as tmux (full text of prompts the TUI truncates), JUMP scrolls Claude's own transcript back to that prompt, BACK TO LIVE returns instantly — try it from a workspace with splits (the walk aims at the focused pane) and confirm a running turn refuses with AGENT IS BUSY.
- UIKIT REBUILD: every surface (deck, terminals, shell, Settings, Add Host, paywall, file viewer, viewport) now runs on UIKit — sweep your usual flows; anything that looks or feels different from the last beta is a find.
- FASTER TAPS: under mouse reporting each tap reaches tmux immediately, one click per tap, so remote double-clicks work.
- VISION PRO GLASS: Settings ▸ Appearance adds GLASS beside SYSTEM / LIGHT / DARK, applied live to deck, terminals, forms, and popovers.
- STABILITY: bytes the app renders but did not author (escape sequences, Sixel/Kitty images, binary output) are now bounded — heavy panes must not crash or bloat.
- SESSION TILE REORDERING: dragging a tile no longer flashes a bright platter, and the drop lands reliably.
- TILE CLICKS ON MAC: a single click on a session tile attaches again; drags now need a brief hold first.
- NOTIFICATION TAPS: pressing an agent alert opens that session — fronting the window that holds it, or attaching it fresh (tmux and herdr alike).
- WIDGET & SHORTCUT AGENTS: work on herdr hosts, and gain a Session choice that launches into an EXISTING session (tmux window / herdr tab or workspace).
- KEYBOARD LOCKED TIP: now sits top-center, clear of the window chrome, with LISTENING replacing it in place.
- VISION PRO POPOVERS: TMUX shortcuts, Command Setup, and agent HISTORY clear the rounded platter's corners.
- AGENT STRIP FOLDS AWAY: tap the strip's agent title (CLAUDE CODE / CODEX / PI) to fold the helper bar into a small agent dot in the bottom-left corner — the terminal reclaims the row; tap the dot to bring the bar back. One choice across all windows, reset on relaunch.
- FILE LINKS: long-press a file:///… address in terminal output; the path sheet should show its decoded path on that SSH host, and ▤ VIEW should open it beside the session in File Viewer. Try a %20 space and a :line suffix.
- HARDWARE KEYBOARD: with one connected, Terminal Actions should omit LOCK KEYBOARD CLOSED; disconnect it and confirm the action returns.
- RIGHT-CLICK: a mouse or trackpad secondary click on terminal text now acts like the long press — a URL or file path under the pointer opens its confirmation sheet, anywhere else raises the SELECT / SELECT ALL / PASTE block (plus MENU on herdr). Needs a real pointer device — try iPad with a trackpad, and links under tmux `mouse on`.
- HERDR PANE MENU: on a herdr tab the long-press block adds MENU — it right-clicks the pressed cell so herdr's own pane menu (Rename / Split / Zoom / Close pane) opens right there; pick with arrow keys or taps as herdr renders it. tmux tabs deliberately don't get it.
- SELECT TEXT: long-press terminal text (tmux and herdr alike) to raise SELECT / SELECT ALL / PASTE right at the press — SELECT hands that word to native iPad selection, SELECT ALL the whole pane; the selection stays inside the pane you pressed on split layouts (focus follows it, as a tap would), one floating block carries the SELECT TEXT lamp + COPY / SELECT ALL / DONE beside the selection (no system menu, no banner), and COPY, DONE, or Escape ends it — while it's on, taps and pans stop reaching the remote app.
- OPEN SOURCE LICENSES: Settings ▸ About ▸ Open Source Licenses lists all 13 shipped third-party components with their complete license text. On iPhone or a narrow iPad sheet, try the grouped registry and open several rows; at regular iPad width or on Vision Pro, try the filterable two-pane component wall. Press COPY and confirm the selected component's full text reaches the pasteboard.
- VISION PRO TERMINAL KEYS: open a terminal with no detected agent and confirm the ESC / CTRL / TAB console row sits wholly below the window instead of straddling its bottom edge; then start an agent and confirm its helper strip still rides above the keys.
- VISION PRO AGENT STRIP MOTION: tap the agent title and its corner dot; the helper slab should now spring smoothly between the full rail and dot instead of snapping between those states.
- LIVE TILE LAYOUT: crowded session telemetry no longer paints over the red LIVE caption; check a running Codex tile on Designed for iPad for a clean state row.
- TERMINAL ACTIONS: on a wide iPad terminal (classic window and full-width shell), connect a hardware keyboard; when no actions remain, the ⋯ button now disappears. Disconnect it and confirm ⋯ returns with LOCK KEYBOARD CLOSED.
- DOUBLE-TAP SELECT: on iPhone/iPad, touch-double-tap terminal text while tmux mouse reporting is on; the SELECT / SELECT ALL / PASTE block should open at that cell. Both taps still reach the mouse-aware remote immediately (preserving single-tap speed); a mouse/trackpad double-click stays wholly remote. On Vision Pro, double-pinch gets the same selection block; a triple tap/pinch must not flash it.
- FILE VIEWER TABS + STICKY DIFF: in + TAB → File Viewer, long-press a file in the tree and choose Open in New Tab; confirm the original viewer stays open and the new tab shows that file. Select DIFF on a changed file, then choose other changed files—the viewer should stay in DIFF until you choose a clean file or SOURCE.
- SORTABLE WINDOW TABS: merge or open at least three tabs, then drag one tab cell toward another and release either on the rail or well below it in the terminal. Confirm the nearest horizontal tab gets the target border, neighboring tabs glide out of the way while the lifted tab settles exactly into its final slot, DROP TO UPLOAD never appears, the active tab stays active, and no terminal reconnects; the same gesture should sort terminal, viewport, and file-viewer tabs on iPhone, iPad, and Vision Pro.
