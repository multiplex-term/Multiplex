# Copy to .env (git-ignored) next to compose.yaml, or export in the shell.
# Same value as fastlane/.env DEMO_SSH_PASSWORD and ASC's demo-account field.
# Long and random; avoid newlines (colons are fine).
DEMO_SSH_PASSWORD=
