# Xcode / Swift
DerivedData/
build/
*.xcodeproj
!project.yml
xcuserdata/
*.xcresult
.swiftpm/
.build/

# macOS
.DS_Store

# Dev harness artifacts (generated keys, sshd runtime state)
Tools/dev-sshd/state/

local-plan/

# fastlane — generated reports and local secrets (see fastlane/SETUP.md)
fastlane/report.xml
fastlane/README.md
fastlane/test_output/
fastlane/keys/
fastlane/.env
# Reviewer contact PII + demo host coordinates, local to the release machine
fastlane/metadata/review_information/
fastlane/multiplex-ios-demo.mp4
# Reviewer demo-host runbook — live-box coordinates, local to the release machine
Tools/review-host/

# App Store screenshot compositor — raw captures in, composed frames out
Tools/appstore/raw/
Tools/appstore/out/
fastlane/screenshots
